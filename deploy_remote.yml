- hosts: wxnacy
  remote_user: root
  tasks:
  - name: git pull /www/wxnacy
    shell: git pull
    args:
      chdir: /www/wxnacy.github.io
  - name: git submodule update yilia
    shell: git submodule update --remote hexo/themes/yilia
    args:
      chdir: /www/wxnacy.github.io
  # - name: deploy wxnacy.github.io python
    # shell: ./deploy_api.sh '{{tag_name}}'
    # args:
      # chdir: ~/wxnacy.github.io
