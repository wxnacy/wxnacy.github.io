#user  nobody;
worker_processes  2;

error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       mime.types;
    default_type  application/octet-stream;

    # log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  logs/access.log;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;

    # server {
        # listen       80;
        # server_name  localhost;

        # #charset koi8-r;

        # #access_log  logs/host.access.log  main;

        # location / {
            # proxy_pass   http://127.0.0.1:4000;
            # proxy_set_header X-Real-IP $remote_addr;
            # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            # proxy_set_header Host $http_host;
					# proxy_set_header X-NginX-Proxy true;
        # }

    # }

    server {
        listen       80;
        server_name  wxnacy.com;
        return       301 https://$server_name$request_uri;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        # location / {
            # # root /www/wxnacy.github.io/hexo/public;
            # # index index.html;
            # rewrite / https://wxnacy.com redirect;
        # }

    }



    # HTTPS server
    #
    server {
        listen 443;
        server_name wxnacy.com;
        ssl on;
        root /www/wxnacy.github.io/hexo/public;
        index index.html;
        ssl_certificate   cert/214107293680722.pem;
        ssl_certificate_key  cert/214107293680722.key;
        ssl_session_timeout 5m;
        ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4;
        ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
        ssl_prefer_server_ciphers on;

        location /api {
            proxy_pass   http://127.0.0.1:5000;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $http_host;
            proxy_set_header X-NginX-Proxy true;
        }

        location / {
            root /www/wxnacy.github.io/hexo/public;
            index index.html;
        }

        # location ^~ /.well-known/acme-challenge/ {
            # alias /www/lets/;
            # try_files $uri =404;

        # }

        location /api/v1 {
            proxy_pass    http://127.0.0.1:8002;
        }

    }

    # server {
        # listen       4002;
        # server_name  wxnacy.com;
        # root   /www/wxnacy.github.io/react-app/build;
        # location / {
            # # index  index.html;
            # try_files $uri /index.html;
            # add_header   Cache-Control public;
            # expires      1d;
        # }
    # }

}
